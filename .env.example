# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# PostgreSQL | SQLServer | MySQL | MongoDB , etc...
# DATABASE_URL="postgresql://johndoe:randompassword@localhost:5432/mydb?schema=public"
# or
# DATABASE_URL="postgresql://opnmyfngbknppm:XXX@ec2-46-137-91-216.eu-west-1.compute.amazonaws.com:5432/d50rgmkqi2ipus?schema=hello-prisma"

# SQLite database: 
# DATABASE_URL="file:<database_path>" is not optional "file:" string
DATABASE_URL="file:/app/db/prod.db"

# Name of domain to use that deploy the container its required before build stage
HOSTNAME="localhost"
# Base URL where the app is deployed
BASE_URL="http://localhost:3000"

# This is de salt to encrypt passwords and jwt.
# keep it safe, since if it is lost, you will not have access to the login in any way.
# Encrypt string 
SECRET="<any_secret>"


# You should not modify these environment variables
# ID of status: answered
ANSWERED_STATUS=1
# ID of status: created
CREATED_STATUS=2
# ID of status: reviewed
REVISED_STATUS=5
# ID of status: updated (for the user)
UPDATED_STATUS=6
